name: 360codelab GCloud SDK
description: |
  Fully fetched GCloud SDK for any use. The rest is ToDo.
inputs:
  credentials:
    description: |
      Base64 json file generated from created project
    required: true
  version:
    description: |
      The version of the GCloud SDK
    default: '328.0.0'
    required: true

runs:
  using: 'node12'
  main: 'index.js'